{% extends 'base.html.twig' %}

{% block title %}Demande index{% endblock %}

{% block body %}
    <h1>Demande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reference</th>
                <th>NumeroActe</th>
                <th>DateSoumission</th>
                <th>Statut</th>
                <th>CopiePdf</th>
                <th>DateTraitement</th>
                <th>LieuTraitement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for demande in demandes %}
            <tr>
                <td>{{ demande.id }}</td>
                <td>{{ demande.reference }}</td>
                <td>{{ demande.numeroActe }}</td>
                <td>{{ demande.dateSoumission ? demande.dateSoumission|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ demande.statut }}</td>
                <td>{{ demande.copiePdf }}</td>
                <td>{{ demande.dateTraitement ? demande.dateTraitement|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ demande.lieuTraitement }}</td>
                <td>
                    <a href="{{ path('app_demande_show', {'id': demande.id}) }}">show</a>
                    <a href="{{ path('app_demande_edit', {'id': demande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_demande_new') }}">Create new</a>
{% endblock %}
